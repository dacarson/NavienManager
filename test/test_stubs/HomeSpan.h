// Minimal stub so PeakFinder.cpp's unused "#include \"HomeSpan.h\"" resolves
// during host-side test compilation, without pulling in the real ESP32-only
// HomeSpan library. PeakFinder.cpp does not actually call anything from it.
#pragma once
